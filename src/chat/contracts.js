/**
 * A request to generate a response for one conversation.
 *
 * @typedef {Object} ConversationRequest
 * @property {import('../platforms/contracts.js').ChannelPort} channel
 * @property {string} botId
 * @property {string|null} [cronMessage]
 */

export {};
